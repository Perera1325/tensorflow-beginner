# ✅ Day 04 - TensorFlow tf.data Pipeline (MNIST)

## What I learned
- Used TensorFlow `tf.data.Dataset`
- Normalized images (0–255 -> 0–1)
- Used batching, shuffling, prefetching
- Trained digit classifier with validation

## Output
- Achieved ~97%+ accuracy on MNIST test set
